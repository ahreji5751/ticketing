name: payments-tests

on:
  pull_request:
    paths: 
      - 'payments/**'
    
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - name: Tests
        run: | 
          cd payments
          npm i
          npm run test:ci
