name: auth-tests

on:
  pull_request:
    branches: [ "master" ]
    paths: 
      - 'auth/**'
    
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3

      - name: Tests
        run: | 
          cd auth
          npm i
          npm run test:ci
